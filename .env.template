#
# Example .env file and dev environment configuration
# Copy this file to .env and modify the values for each environment
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
#

# +--------------------+
# |       COMMON       |
# +--------------------+

ENVIRONMENT=development

# +--------------------+
# |       NODEJS       |
# +--------------------+

NODE_ENV=development

# +--------------------+
# |       PYTHON       |
# +--------------------+

LOG_LEVEL="debug"

SYSTEM_USER_ID="8d35b4fb-16f1-4d38-b592-580eaec56a99"
BACKEND_SENTRY_DSN="https://example@sentry.com/27"
MJML_BINARY="C:\Users\ggroz\AppData\Roaming\npm\mjml.cmd"

# Security

# <base64-encoded 32 raw bytes>, generate with `openssl rand -base64 32`
JWT_SECRET_KEY="v4f8kfJ6tsENnzHrvu4ynR9aZ/A8McG0bte5x8iZFig="
# or RS256 if you switch to RSA keys later
JWT_ALGORITHM=HS256
# optional but recommended
JWT_ISSUER=https://api.jarvis.local

# Opaque session token encryption key, <base64-encoded 32 raw bytes>, generate with `openssl rand -base64 32`
USER_SIGNATURE_PRIVATE_KEY="flaGLa4Ud+5TT9JJ3KaIV8uaXrXjOFFoPL3QLyJ0NRE="
# Optional fallback key during rotation. Keep the current key above, move the old value here,
# wait until old refresh tokens have expired, then remove this variable.
# Intentionally spelled PREVIUS to match existing deployment tooling expectations.
# USER_SIGNATURE_PRIVATE_KEY_PREVIUS=""
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_MINUTES=4320
VERIFICATION_TOKEN_EXPIRE_HOURS=24
BCRYPT_ROUNDS=12
SOCIAL_LINK_TOKEN_TTL_MINUTES=30
# Refresh token HttpOnly cookie settings
REFRESH_COOKIE_NAME="jarvis_refresh"
REFRESH_COOKIE_PATH="/user/auth"
# Leave empty to keep cookie host-only (recommended for api.jarvis.local)
REFRESH_COOKIE_DOMAIN=""
# Use lax by default; set none only if cross-site embedding is required (must keep secure=true then)
REFRESH_COOKIE_SAMESITE="lax"
# Optional override; defaults to true in stage/prod and false in dev
# REFRESH_COOKIE_SECURE=true

# Google OAuth
GOOGLE_OAUTH_CLIENT_ID="exampleId.apps.googleusercontent.com"
GOOGLE_OAUTH_CLIENT_SECRET="GOCSPX-exampleSecret"
GOOGLE_TAG_ID="G-EXAMPLE1234"

# API Keys
GOOGLE_API_KEY="AIza_exampleKey"
# Optional overrides for the Gemini integration
# ADMIN_CREATIVE_GEMINI_TEXT_MODEL="gemini-3.1-flash-preview"
# ADMIN_CREATIVE_GEMINI_TEXT_FALLBACK_MODEL="gemini-3-flash-preview"
# GEMINI_GENERATE_CONTENT_BASE_URL="https://generativelanguage.googleapis.com/v1beta"
HETZNER_API_TOKEN="exampleToken"

# Email
MAIL_USERNAME="system@jarvis.local"
MAIL_PASSWORD="app-specific-password"
MAIL_SERVER="smtp.gmail.com"
MAIL_PORT=587
MAIL_FROM="system@jarvis.local"
MAIL_FROM_NAME="Jarvis"
MAIL_PROVIDER="google"
CRITICAL_ISSUE_INBOX="system@jarvis.local"

# System-wide usage caps (0 disables the limit)
SYSTEM_LIMIT_GEMINI_DAILY_CALLS=0
SYSTEM_LIMIT_GEMINI_MONTHLY_CALLS=0
SYSTEM_LIMIT_USER_REG_DAILY=0
SYSTEM_LIMIT_USER_REG_MONTHLY=0
SYSTEM_LIMIT_STORAGE_DAILY_GB=0
SYSTEM_LIMIT_STORAGE_MONTHLY_GB=0

# Database Configuration
DB_NAME="Jarvis"
DB_USERNAME="admin"
DB_PASSWORD="admin"
DB_HOSTNAME="database"
DB_PORT="5432"

# Storage Configuration
STORAGE_PROVIDER=s3
# For Hetzner Object Storage prefer https and leave STORAGE_PORT empty.
# Example: STORAGE_HOSTNAME=https://nbg1.your-objectstorage.com
# For Docker Compose local development use http://minio.
# For a backend running directly on the host against Compose MinIO, use http://localhost.
STORAGE_HOSTNAME=http://minio
STORAGE_EXTERNAL_HOSTNAME=http://localhost
STORAGE_PUBLIC_BUCKET=default
STORAGE_PRIVATE_BUCKET=private
STORAGE_KEY=nd4w0MqGXamtyYQPvCPF
STORAGE_SECRET=edQThiWoEFmaTSJYZJUpGTAUYhm09tmmC7ELZnHA
STORAGE_PORT=9700
STORAGE_STARTUP_ATTEMPTS=3
STORAGE_STARTUP_RETRY_DELAY=1
# Keep false in production to avoid writing/reading objects from local disk fallback.
STORAGE_ALLOW_LOCAL_FALLBACK=false
LOCAL_STORAGE_PATH=/code/storage


# +--------------------+
# |      POSTGRES      |
# +--------------------+

# Database configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=jarvis
PGTZ="UTC"
